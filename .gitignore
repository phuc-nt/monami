# === Secrets — never commit ===
.env
.env.*
!.env.example
*.pem
*.key

# === Python (local envs + caches — never commit) ===
.pyenv-spike/
.pyenv-backend/
.venv/
venv/
__pycache__/
*.pyc

# === Private: child speech recordings — NEVER commit (privacy) ===
# Keep harness code + templates; ignore ALL audio + transcribed logs everywhere.
test/test-input-audio/
spike/results/*.jsonl
spike/results/audio_out/
spike/samples/
*.wav
*.pcm
*.aiff
*.m4a
*.mp3
*.ogg
*.caf

# === Local agent config / instructions — not for remote ===
.claude/
AGENTS.md
CLAUDE.md

# === OS / editor noise ===
.DS_Store
*.swp
*~

# === JS/TS deps (if ever added) ===
node_modules/
dist/
build/
